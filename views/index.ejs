<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>课程列表</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            padding: 20px;
            background: #f5f5f5;
        }
        .table-container {
            background: #fff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            margin-bottom: 80px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #eee;
        }
        th {
            background: #f7f7f7;
            font-weight: 500;
            color: #333;
        }
        tr:hover {
            background: #f9f9f9;
        }
        .product-link {
            color: #1890ff;
            text-decoration: none;
            cursor: pointer;
            display: block;
            padding: 5px 0;
        }
        .product-link:hover {
            color: #40a9ff;
            text-decoration: underline;
        }
        .price {
            color: #ff4d4f;
            font-weight: 500;
        }
        .thumbnail {
            width: 60px;
            height: 60px;
            object-fit: cover;
            border-radius: 4px;
        }
        .loading {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 15px 30px;
            border-radius: 4px;
            z-index: 1000;
        }
    </style>
</head>
<body>
    <div class="loading" id="loading">加载中...</div>
    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>缩略图</th>
                    <th>商品名称</th>
                    <th>课程时间</th>
                    <th>价格</th>
                    <th>开课人数</th>
                    <th>已售数量</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach(item => { %>
                    <tr>
                        <td>
                            <img src="<%= item.fields.缩略图?.link || '' %>" alt="<%= item.fields.商品名称 || '' %>" class="thumbnail">
                        </td>
                        <td>
                            <a class="product-link" onclick="showProductDetail('<%= encodeURIComponent(JSON.stringify(item.fields)) %>')">
                                <%= item.fields.商品名称 || '' %>
                            </a>
                        </td>
                        <td><%= item.fields.上课时间 || '' %></td>
                        <td class="price">¥<%= item.fields.会员价格 || '0' %></td>
                        <td><%= (item.fields.最少上课人数 || '0') %>-<%= (item.fields.最大上课人数 || '0') %>人</td>
                        <td><%= item.fields.已售数量 || '0' %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>

    <script>
        function showProductDetail(productData) {
            document.getElementById('loading').style.display = 'block';
            try {
                window.location.href = `/product/detail?data=${productData}`;
            } catch (error) {
                console.error('跳转错误:', error);
                document.getElementById('loading').style.display = 'none';
                alert('页面跳转失败，请稍后重试');
            }
        }
    </script>
</body>
</html> 